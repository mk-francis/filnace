## 目标
- 为不同分公司配置各自的费用类别；录入弹窗与筛选chips按所选公司动态展示对应类别。

## 类别配置（差异化）
- ABC Auto Repair Shop（修车行）：修车、零件、维保、空调、工资、其他
- Custom Motors PH（改装公司）：改装、零件、维保、工资、其他
- CoolTech Aircon Services（空调服务）：空调安装、空调维修、设备采购、维保、工资、其他

## 实施点
- 更新 `assets/js/store.js` 中 `categoriesByCompany` 映射为上述集合；保留 `getCompanyCategories(companyId)` 提供给前端。
- 录入弹窗已两级联动，更新后自动生效；校验仍按公司类别集合进行。
- 历史记录中的旧类别不受影响；仅新增/编辑会按新集合校验。

## 验证
- 清空 `localStorage` 并硬刷新；分别选择三家分公司打开弹窗，确认类别列表不同且符合上述配置；提交后数据生效。