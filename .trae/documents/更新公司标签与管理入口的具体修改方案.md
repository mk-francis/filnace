## 变更点

* 顶部标签改为“全部 / 收入 / 支出 / 分公司名称”，点击公司标签切换公司视图；在“全部分公司”仅显示前三项。

* 录入弹窗保持“两级选择（分公司→类别）”，类别随公司联动与校验。

* 新增“公司与类别管理”入口（settings.html），支持增删改子公司与其差异化类别。

## 文件与实现

* assets/js/store.js：新增/完善 `addCompany`、`updateCompany`、`removeCompany`、`setCompanyCategories`、`addCompanyCategory`、`removeCompanyCategory`；保留 `getCompanies` 与 `getCompanyCategories` 提供数据。

* transactions.html：

  * 增加chips容器并用 `Store.getCompanies()` 动态渲染公司标签；保留“全部/收入/支出”。

  * 事件：点击公司→设置 `currentCompany` 并刷新列表；点击收入/支出→按 `type` 过滤；点击全部→回到集团视图。

* index.html：同样渲染并联动公司标签；点击公司切换仪表与最近交易过滤。

* settings.html（新页）：

  * 公司列表与“新建公司”模态；右侧编辑公司与类别chips（添加/删除/排序）；保存调用 Store 方法。

  * 侧边栏增加管理入口。

## 验证

1. chips显示每个分公司；点击公司标签切换视图。
2. 管理页新增/修改/删除公司与类别；返回录入弹窗与chips显示最新配置；提交成功。
3. 在“全部分公司”仅显示“全部/收入/支出”。

## 交付

* 修改上述文件并完成回归测试；提供操作说明与截图验证。

